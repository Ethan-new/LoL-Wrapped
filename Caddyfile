# IP-only access (no domain): serves HTTP on port 80
# Access via http://YOUR_SERVER_IP
#
# With a domain: replace :80 with your domain (e.g. lol-wrapped.example.com)
# Then add a DNS A record pointing to your server IP for HTTPS.

:80 {
    reverse_proxy web:3000
}
