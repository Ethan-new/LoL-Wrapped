# Copy to .env and fill in your values.
# Get a Riot API key: https://developer.riotgames.com/

RIOT_API_KEY=your_riot_api_key_here
REDIS_URL=redis://localhost:6379/0

# Optional: delay (seconds) between match-detail API calls during ingest. Default 1.2.
# Increase if rate limited (e.g. 2.0). Decrease for faster ingest with production key (e.g. 0.5).
# RIOT_MATCH_DELAY=1.2
