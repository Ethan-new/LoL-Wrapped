<% content_for :title, "LoL Wrapped - Player Lookup" %>

<div class="flex w-full max-w-2xl flex-col items-center">
  <header class="text-center">
    <h1 class="text-4xl font-bold tracking-tight text-white">LoL Wrapped</h1>
    <p class="mt-2 text-lg text-zinc-400">Search your game name to get your <%= Time.current.year - 1 %> wrapped</p>
  </header>

  <div class="w-full">
    <%= render "lookup_result", error: error, details: details, show_region_hint: show_region_hint %>
  </div>
</div>
